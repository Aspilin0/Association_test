# 运行时依赖（当前环境已内置 aiohttp/httpx/pydantic/dotenv）
# 说明：因无外网，暂用 aiohttp 作为 HTTP 层；联网后可改回 fastapi + uvicorn。
aiohttp>=3.9
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0

# 可选（联网后启用 FastAPI 版本）
# fastapi>=0.110
# uvicorn[standard]>=0.29
